{
  "name": "vacation-schedule-site",
  "version": "1.0.0",
  "description": "A reusable, data-driven template for a password-protected family trip itinerary site.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.js && node scripts/serve.js",
    "build": "node scripts/build.js",
    "validate": "node scripts/validate.js"
  },
  "license": "MIT",
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "dotenv": "^16.4.5"
  }
}
